# Bản đồ vùng code theo nghiệp vụ — cho `agy-scout.sh --scope`

> Mỗi dòng: alias nghiệp vụ → thư mục/file → 1–3 câu nghiệp vụ và bẫy worker cần biết trước khi đọc code. Cột nghiệp vụ được nạp thẳng vào prompt scout. Ai đổi cấu trúc thư mục hoặc chốt nghiệp vụ mới thì sửa dòng tương ứng.

Site: `https://type.scala.vn/` — app luyện gõ 10 ngón tiếng Việt cho trẻ em (Vite + React 19 + react-router v6, SSG bằng `vite-react-ssg`, deploy Vercel). Tool luyện gõ là desktop-only; 4 trang guide phải đọc tốt trên mobile.

| Alias | Đường dẫn | Nghiệp vụ / bẫy |
|---|---|---|
| `seo` | `src/components/Seo.tsx` `src/components/JsonLd.tsx` `src/components/guide-schema.ts` `src/components/RelatedGuides.tsx` `src/routes.tsx` `src/main.tsx` `index.html` `scripts/gen-sitemap.mjs` `public/robots.txt` `vercel.json` `vite.config.ts` | Hạ tầng SEO: meta/title/canonical render per-route qua `<Head>` của vite-react-ssg (KHÔNG dùng React 19 auto-hoist khi SSG). JSON-LD: `index.html` chỉ giữ SoftwareApplication; FAQPage/Breadcrumb/Article render per-route qua `<JsonLd>`. Sitemap sinh lúc build: `scripts/gen-sitemap.mjs` chạy ở `postbuild`, lastmod = ngày commit gần nhất của file nguồn trang (bảng `ROUTES`); `public/sitemap.xml` đã xóa. Thêm trang mới = thêm 1 dòng vào `ROUTES`. `vercel.json` `cleanUrls` + rewrite mọi route (trừ `/assets/`) về `index.html`; file tĩnh SSG ưu tiên trước rewrite; từ deploy 06/09 URL lạ trả 404 thật (Vercel text, chưa có `404.html`). Bẫy: commit chưa push ≠ đã deploy — verify bằng `curl` live trước khi tick plan; Vercel nối Git, push `main` = deploy prod. FAQPage của trang chủ nằm ở `src/components/home-faq.ts`, render bằng `<JsonLd>` chỉ trong route `/` (trước đây tĩnh trong `index.html` nên lặp lên mọi route) — `index.html` chỉ còn SoftwareApplication. |
| `guides` | `src/pages` | 6 trang nội dung: 4 guide (`/huong-dan-telex`, `/huong-dan-vni`, `/tu-the-go-phim`, `/bi-mat-phim-f-j`) + 2 trang bảng tra cứu in được (`/bang-go-telex`, `/bang-go-vni`, thêm 09/2026). Dữ liệu bảng nguyên âm × thanh dùng chung ở `src/pages/typing-table-data.ts` (`buildToneTable(rules)`, `TONE_LABELS`, `cellStyle`, `headStyle`) — 4 trang đọc cùng nguồn, đừng copy lại. Trang bảng có nút `window.print()` + CSS `@media print` ở cuối `src/index.css` (class `.no-print` để ẩn khi in). Đã tối ưu on-page keyword (phase 03, 07/2026). Style inline, ảnh ở `public/guides/` (có `width`/`height` + `height:auto`, KHÔNG lazy vì là ứng viên LCP). Bẫy: `#root` là flex column nên wrapper trang guide phải có `width: '100%'` cạnh `maxWidth: '800px'` — thiếu nó thì bảng `minWidth: 560px` kéo cả trang rộng 600px và cuộn ngang ở 375px (đã sửa 06/09). Bảng rộng luôn bọc trong `<div style={{ overflowX: 'auto' }}>`. |
| `home` | `src/components/HomePage.tsx` `src/components/HomeView.tsx` `src/components/RootLayout.tsx` `src/components/DesktopNudge.tsx` | Trang chủ `/`: landing + chọn lesson/game; trên ≤768px thay tool luyện gõ bằng nudge "mở trên máy tính". `RootLayout` chứa `<Analytics/>` Vercel. |
| `practice` | `src/components/PracticeSession.tsx` `src/components/GameSession.tsx` `src/components/TypingArea.tsx` `src/components/Keyboard.tsx` `src/components/Hands.tsx` `src/components/Stats.tsx` `src/components/LessonSidebar.tsx` `src/hooks` `src/utils` `src/constants.ts` | Engine luyện gõ + mini game (Telex/VNI tone matching trong `src/utils/typing-engine.ts`, có test vitest). Không liên quan SEO — không scout cho việc SEO. |
